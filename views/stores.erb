<div class="container">
  <h1>Stores in your distribution network</h1>
  <ul>
    <% @stores.each() do |store| %>
      <li><%= store.name() %></li>
      <ul>
        <% self.shoes.each() do |shoe| %>
        <li><%= shoe.name() %></li>
      </ul>
    <% end %>
  </ul>
</div>
